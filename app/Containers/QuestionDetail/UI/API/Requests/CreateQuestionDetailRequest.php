<?php

namespace App\Containers\QuestionDetail\UI\API\Requests;

use App\Ship\Parents\Requests\Request;

/**
 * Class CreateQuestionDetailRequest.
 */
class CreateQuestionDetailRequest extends Request
{
    /**
     * Define which Roles and/or Permissions has access to this request.
     *
     * @var  array
     */
    protected $access = [
        'permissions' => '',
        'roles'       => '',
    ];

    /**
     * Id's that needs decoding before applying the validation rules.
     *
     * @var  array
     */
    protected $decode = [
        'plan_depart_question_id',
    ];

    /**
     * Defining the URL parameters (e.g, `/user/{id}`) allows applying
     * validation rules on them and allows accessing them like request data.
     *
     * @var  array
     */
    protected $urlParameters = [

    ];

    /**
     * @return  array
     */
    public function rules()
    {
        return [
            'plan_depart_question_id' => 'required|exists:plan_depart_questions,id',
            'question'                => 'required|max:255',
            'answer'                  => 'required',
        ];
    }

    /**
     * @return  bool
     */
    public function authorize()
    {
        return $this->check([
            'hasAccess',
        ]);
    }
}
